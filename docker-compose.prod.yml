services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: streamsight_db
    networks:
      - streamsight-network
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build:
      context: ./streamsight-studio-backend
      additional_contexts:
        streamsight: ../streamsight
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - ./streamsight-studio-backend/src:/src/src
    env_file: .env
    environment:
      PYTHONUNBUFFERED: 1
      DATABASE_URL: postgresql+psycopg://postgres:password@postgres:5432/streamsight_db
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - streamsight-network
    develop:
      watch:
        - action: sync+restart
          path: ./streamsight-studio-backend/src
          target: /src/src
          ignore:
            - __pycache__
            - .pytest_cache
            - .venv
            - .egg-info
            - dist

  frontend:
    build:
      context: ./streamsight-studio-frontend
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    volumes:
      - ./streamsight-studio-frontend:/app
      - /app/node_modules
    env_file: .env
    depends_on:
      - backend
    networks:
      - streamsight-network
    restart: unless-stopped

volumes:
  pgdata:

networks:
  streamsight-network:
    driver: bridge
